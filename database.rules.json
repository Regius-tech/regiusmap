{
  "rules": {
    "companies": { ".read": true, ".write": false },
    "positions": { ".read": true, ".write": false },
    "users": {
      "$uid": { ".read": "$uid === auth.uid", ".write": "$uid === auth.uid" }
    },
    "cars": { ".read": true, ".write": true }
  }
}
